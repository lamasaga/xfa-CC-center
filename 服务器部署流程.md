# 服务器部署流程

　　本文档说明如何将本仓库的更新发布到线上服务器，供日常开发与运维查阅。更细的数据库备份、JWT 配置、Nginx 初始化等内容见 `doc/` 目录下的专题文档。

---

## 一、线上架构

```text
浏览器
  → HTTP/HTTPS(80/443)
  → Nginx（宿主机）
  → http://127.0.0.1:3001
  → Node/Express（主站 SPA + /api + 院校探索静态站）
  → SQLite（database.sqlite）
```

| 组件 | 本地目录 | 服务器路径 | 说明 |
|------|----------|------------|------|
| 主站（升学管理系统） | `app/` | `/opt/alevelinfo/app` | React + Vite 构建后由 Express 托管 |
| 院校探索站 | `study-app/` → 构建产物写入 `app/study-dist/` | `/opt/alevelinfo/app/study-dist/` | 同域子路径 `/study/` |
| 进程管理 | — | `systemd` 单元 `alevelinfo` | 启停、环境变量（含 `JWT_SECRET`） |
| 数据库 | `app/database.sqlite`（仅本地开发） | `/opt/alevelinfo/app/database.sqlite` | **线上库独立存在，默认不随部署覆盖** |

**当前服务器（示例）**

| 项 | 值 |
|----|-----|
| 公网 IP | `42.193.112.229` |
| SSH 用户 | `ubuntu` |
| SSH 私钥 | `%USERPROFILE%\.ssh\alevelinfo_ed25519` |
| 应用目录 | `/opt/alevelinfo/app` |
| 服务名 | `alevelinfo` |

---

## 二、目录易混淆说明

| 目录 | 是否部署到服务器 | 说明 |
|------|------------------|------|
| `app/` | **是** | 主站源码；部署包以此为核心 |
| `study-app/` | **是（经构建）** | 院校探索站源码；须先 `npm run build`，产物复制到 `app/study-dist/` |
| `university/` | **否** | 与 `study-app` 同类的本地副本，**不会**被部署脚本使用；改动需同步到 `study-app` 后再发布 |
| `deploy/` | 部分 | 含 `upload-and-update.ps1`、`remote-update.sh`，本机执行或上传到服务器临时目录 |

---

## 三、前置条件

### 3.1 本机（Windows）

- 已安装 **OpenSSH 客户端**（`ssh`、`scp`）
- 已配置 SSH 私钥，且能无密码登录服务器：

```powershell
ssh -i "$env:USERPROFILE\.ssh\alevelinfo_ed25519" ubuntu@42.193.112.229 "echo OK"
```

- 本机 **Node.js + npm**（用于构建 `study-app`；主站构建在服务器执行）
- Windows 自带 **`tar.exe`**（部署脚本打包用）

### 3.2 服务器（Ubuntu）

- 已安装 Node.js、npm
- 已配置 `systemd` 服务 `alevelinfo`，且 `JWT_SECRET` 等环境变量写在 `/etc/systemd/system/alevelinfo.service` 中（**不要**依赖仓库里的 `.env` 上传）
- `better-sqlite3` 在服务器上由 `npm rebuild` 生成 Linux 原生模块（**禁止**把本机 `node_modules` 整包上传）

---

## 四、标准一键部署（推荐）

　　适用于：主站 `app/` 有改动，或需要完整、可复现的一次发布。

### 4.1 命令

在 **PowerShell** 中于仓库根目录执行：

```powershell
Set-Location "D:\1XFAwork\highSCHOOL\A-levelCENTER\Alevelinfo"
.\deploy\upload-and-update.ps1 `
  -Server "42.193.112.229" `
  -IdentityFile "$env:USERPROFILE\.ssh\alevelinfo_ed25519"
```

可选参数（一般保持默认即可）：

| 参数 | 默认值 | 含义 |
|------|--------|------|
| `-User` | `ubuntu` | SSH 登录用户 |
| `-RemoteAppPath` | `/opt/alevelinfo/app` | 服务器应用目录 |
| `-ServiceName` | `alevelinfo` | systemd 服务名 |

### 4.2 脚本实际做了什么

**本机阶段**

1. 进入 `study-app/`，执行 `npm ci` 与 `npm run build`（`base` 为 `/study/`）
2. 将 `study-app/dist` 复制到 `app/study-dist/`（覆盖旧静态资源）
3. 在 `app/` 目录打包 `alevelinfo-deploy.tgz`，**排除**：`node_modules`、`dist`、本地 `database.sqlite*`、`uploads`、`.env`、`.git`
4. 通过 `scp` 上传压缩包与 `deploy/remote-update.sh` 到服务器 `/tmp/`

**服务器阶段**（`remote-update.sh`）

1. `systemctl stop alevelinfo`（避免文件占用）
2. 若存在 `database.sqlite`，自动备份为 `database.sqlite.bak-日期时间`
3. 解压覆盖 `/opt/alevelinfo/app`（未打包的文件如线上库、uploads 保留）
4. `npm ci` → `npm run build` → `npm rebuild better-sqlite3`
5. `systemctl start alevelinfo`，并检查服务是否为 `active`

### 4.3 预计耗时

- 本机 `study-app` 构建：约 0.5～2 分钟
- 上传：视网络而定
- 服务器 `npm ci` + 构建：约 1～3 分钟

---

## 五、仅更新院校探索站（study-app）

　　适用于：只改了 `study-app/`（页面、样式、`universities_data.json` 等），主站 `app/` 无变更。可跳过完整打包，只同步静态文件，**无需重启服务**（Express 直接读磁盘上的 `study-dist`）。

### 5.1 本机构建并同步到 app/study-dist

```powershell
Set-Location "D:\1XFAwork\highSCHOOL\A-levelCENTER\Alevelinfo\study-app"
npm run build

$StudyBuild = "D:\1XFAwork\highSCHOOL\A-levelCENTER\Alevelinfo\study-app\dist"
$StudyDist  = "D:\1XFAwork\highSCHOOL\A-levelCENTER\Alevelinfo\app\study-dist"
if (Test-Path $StudyDist) { Remove-Item $StudyDist -Recurse -Force }
Copy-Item $StudyBuild $StudyDist -Recurse
```

### 5.2 上传到服务器

```powershell
$key    = "$env:USERPROFILE\.ssh\alevelinfo_ed25519"
$server = "ubuntu@42.193.112.229"
$local  = "D:\1XFAwork\highSCHOOL\A-levelCENTER\Alevelinfo\app\study-dist"

ssh -i $key $server "rm -rf /opt/alevelinfo/app/study-dist"
scp -i $key -r $local "${server}:/opt/alevelinfo/app/"
```

### 5.3 浏览器验证

访问主站顶栏「院校库」或 `https://你的域名/study/`；若样式未更新，使用 **Ctrl+F5** 强制刷新。

---

## 六、部署后验证

### 6.1 本机通过 SSH 检查（在服务器上执行）

```bash
sudo systemctl status alevelinfo --no-pager -l
curl -sS -o /dev/null -w "%{http_code}\n" http://127.0.0.1:3001/
curl -sS -o /dev/null -w "%{http_code}\n" http://127.0.0.1:3001/study/
sudo journalctl -u alevelinfo -n 40 --no-pager
```

期望：主站与 `/study/` 均返回 `200`，服务状态为 `active (running)`。

### 6.2 从 Windows 一键远程检查

```powershell
$key = "$env:USERPROFILE\.ssh\alevelinfo_ed25519"
ssh -i $key ubuntu@42.193.112.229 "curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:3001/study/"
```

---

## 七、不会上传 / 不会覆盖的内容

| 内容 | 说明 |
|------|------|
| `app/database.sqlite` | 线上用户与升学数据；部署包刻意排除，远端脚本仅做备份不替换 |
| `.env` / `JWT_SECRET` | 生产密钥只在服务器 `systemd` 中配置 |
| `node_modules` | 各环境在目标机器上 `npm ci` 安装 |
| `app/dist` | 主站构建产物在服务器重新生成 |
| `university/` | 非发布目录 |
| `studentapp/` | 非发布目录 |

---

## 八、常见问题

### 8.1 本机 study-app 构建失败：`EPERM` / `tsc` 找不到

**现象**：`npm ci` 报 `esbuild.exe` 无法删除；随后 `tsc` 不是内部命令。

**原因**：`node_modules` 损坏或被占用（杀毒、残留进程等）。

**处理**：

```powershell
Set-Location "D:\1XFAwork\highSCHOOL\A-levelCENTER\Alevelinfo\study-app"
Remove-Item node_modules -Recurse -Force -ErrorAction SilentlyContinue
npm install
npm run build
```

　　若一键脚本在本机构建失败但旧 `dist` 仍存在，脚本可能继续上传**过期的** `study-dist`。发布前务必确认本机 `npm run build` 成功，或按第五节单独同步。

### 8.2 脚本显示「全部完成」但网站未更新

以服务器实际状态为准：

```bash
sudo systemctl status alevelinfo -l
sudo journalctl -u alevelinfo -n 50 --no-pager
```

### 8.3 服务反复重启 / 3001 连接被拒绝

多为生产环境未配置合法 **`JWT_SECRET`**。在服务器检查：

```bash
sudo systemctl show alevelinfo -p Environment --no-pager
```

配置与重启方法见 `doc/线上更新与数据库维护纪要-2026-04-21.md` 第二节。

### 8.4 `better-sqlite3` 报错

在服务器应用目录执行：

```bash
cd /opt/alevelinfo/app
npm rebuild better-sqlite3
sudo systemctl restart alevelinfo
```

### 8.5 PowerShell 脚本中文乱码或解析错误

`deploy/upload-and-update.ps1` 建议保存为 **带 BOM 的 UTF-8**（PowerShell 5.1 兼容）。

---

## 九、数据与账号维护（简要）

| 操作 | 说明 |
|------|------|
| 重置线上教务账号口令 | SSH 登录后在 `/opt/alevelinfo/app` 执行 `npm run reset-staff-passwords` |
| 数据库备份 | 见 `doc/服务器数据库备份与维护.md` |
| 探索站 JSON 与 SQLite 业务库 | 当前为双数据源；改 `study-app/public/*.json` 只影响探索展示，雷达图等仍读 API 库 |

---

## 十、相关文件与文档

| 路径 | 用途 |
|------|------|
| `deploy/upload-and-update.ps1` | 本机一键部署入口 |
| `deploy/remote-update.sh` | 服务器端解压、构建、重启 |
| `app/server/index.js` | `/study` 静态托管逻辑 |
| `study-app/vite.config.ts` | 生产 `base: '/study/'` |
| `doc/部署指南.md` | 首次装机、Nginx、Docker 等完整指南 |
| `doc/线上更新与数据库维护纪要-2026-04-21.md` | JWT、数据库与上传关系 |
| `doc/服务器数据库备份与维护.md` | SQLite 热备、cron |
| `doc/部署复盘-2026-04-14.md` | 首次上线踩坑记录 |
| `院校库与study-app集成方案.md` | 院校探索站与主站集成设计 |

---

## 十一、发布检查清单

- [ ] 改动已落在正确目录（探索站 → `study-app/`，非 `university/`）
- [ ] 本机 `study-app` 的 `npm run build` 成功
- [ ] 若改主站，确认 `app/` 相关文件已保存
- [ ] 执行 `deploy/upload-and-update.ps1` 或按需仅同步 `study-dist`
- [ ] 服务器 `systemctl status alevelinfo` 为 `active`
- [ ] 浏览器访问主站与 `/study/` 功能正常
- [ ] 未将 `.env`、数据库备份、口令文件提交到 Git
